# Farley Flooring Timecard — Backend (Render)

## Env Vars (Render)
- `MAIL_USER` = farleyflooring.timecards@gmail.com
- `MAIL_PASS` = <Gmail App Password>

## Start
```
npm install
npm start
```
Render will auto-detect Node and run `npm start`.
